import React from 'react';

import GuestInfo from './GuestInfo';

const UserInfo = () => {
  return (
    <div>
      <h2>Set your user info:</h2>
      <GuestInfo />
    </div>
  );
};

export default UserInfo;
